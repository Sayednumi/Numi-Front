{
  "name": "numi-platform",
  "version": "1.0.0",
  "description": "Numi Educational Platform Root Package for Deployment",
  "scripts": {
    "start": "node backend/server.js",
    "postinstall": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
